# Grafana datasource provisioning.
#
# Automatically configures Prometheus as the default datasource when
# Grafana starts. No manual setup required.

apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
